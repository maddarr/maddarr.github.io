import assert from 'assert';
const assert = require('assert');
const employmentChoice = require('./index');

describe('employmentChoice', () => {
    describe('choiceId', () => {
        it('returns a style height', () => {
            const expectedResult = '2px';
            assert.strictEqual(expectedResult);
        });
    });
});